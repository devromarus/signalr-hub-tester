# Логи разработки

## 2026-08-12

- [09:22] Начало работы: устранение ошибки сборки Vite "Could not resolve @alfalab/core-components-stack-context/esm".
- [09:22] Диагностика: пакет `@alfalab/core-components-stack-context@1.0.1` объявлен как peerDependency в `@alfalab/core-components`, `toast`, `stack`, `popover`, `base-modal`, но отсутствовал в `node_modules` и не был добавлен как прямая зависимость проекта.
- [09:22] Изменение: добавлена зависимость `@alfalab/core-components-stack-context: ^1.0.1` в `dependencies` файла `package.json`.
- [09:22] Установка: выполнена команда `yarn install --ignore-engines` (обход проверки engines, т.к. установлена Node 22.14.0 вместо требуемой 22.22.0). Пакет установлен, `esm/index.js` экспортирует `StackingContext` и `stackingOrder`.
- [09:23] Проверка: команда `npx vite build` завершена успешно (1418 модулей). Ошибки резолва `@alfalab/core-components-stack-context/esm` устранены.
- [10:22] Начало работы: исправление трёх проблем — отображение `null` при успешном вызове, очистка полей при смене вкладок, отсутствие тела запроса в журнале.
- [10:22] Изменение `components/MethodInvoker/MethodInvoker.tsx`: при успешном вызове без возвращаемого значения (`result === undefined`) отображается сообщение «Метод выполнен без возвращаемого значения» вместо `null`.
- [10:23] Изменение `App.module.css`: добавлен класс `.hidden` (`display: none`) для скрытия неактивных вкладок.
- [10:24] Изменение `App.tsx`: вкладки больше не размонтируются при переключении — все остаются в DOM, неактивные скрываются через CSS. Состояние полей сохраняется.
- [10:24] Изменение `App.tsx` (`handleInvoke`): в журнал добавлено тело запроса (`{ method, args }`) вместе с ответом.
- [10:25] Проверка: `npx vite build` завершена успешно. Приложение запущено в dev-режиме, вкладки переключаются корректно.
- [10:25] Остановка: dev-сервер на порту 5173 остановлен (`lsof -ti:5173 | xargs kill`), порт освобождён для предотвращения конфликтов.
- [10:25] Конец работы: задача выполнена.
- [14:12] Исправление `components/MethodInvoker/MethodInvoker.tsx`: проблема с `null` при успешном ответе хаба. Хаб возвращает `null` (а не `undefined`) для методов без возвращаемого значения. Условие расширено: `result.result === undefined || result.result === null` — теперь отображается «Метод выполнен без возвращаемого значения».
- [14:13] Проверка: `npx vite build` завершена успешно.

## 2026-08-13

- [11:27] Начало работы: добавление favicon в стиле Альфа-Банка.
- [11:27] Создание `public/favicon.svg`: фирменный красный фон `#EF3124` (круглая форма) и белая буква «А» — официальный SVG-путь из пакета `@alfalab/icons-logotype` (`AlfaBankLogoMIcon`).
- [11:27] Изменение `index.html`: добавлен тег `<link rel="icon" type="image/svg+xml" href="/favicon.svg" />` в `<head>`.
- [11:27] Конец работы: задача выполнена.
- [11:36] Начало работы: подготовка Docker-развёртывания приложения.
- [11:36] Создание `Dockerfile`: multi-stage сборка — stage `build` (`node:22-alpine`, `npm ci`, `npm run build`), stage `runtime` (`nginx:alpine`, копирование `dist` и `nginx.conf`). Добавлен HEALTHCHECK.
- [11:36] Создание `nginx.conf`: статическая раздача SPA с fallback на `index.html`, gzip-сжатие, кэширование ассетов из `/assets/` и изображений.
- [11:36] Создание `docker-compose.yml`: сервис `web`, проброс порта `8080:80`, `restart: unless-stopped`, healthcheck.
- [11:36] Создание `.dockerignore`: исключены `node_modules`, `dist`, `.git`, документация и локальные файлы из контекста сборки.
- [11:36] Изменение `README.md`: добавлен раздел «Запуск через Docker» с командами запуска/остановки и описанием Docker-файлов.
- [11:36] Конец работы: задача выполнена.
- [11:41] Ошибка: `docker compose up -d --build` упал на шаге `npm ci` (exit code 1) с ошибкой npm "Exit handler never called!". Причина — конфликт peer-зависимостей: `react-virtual@2.10.4` (внутри `@alfalab/core-components-select`) требует `react@^16.6.3 || ^17.0.0`, а проект использует `react@18.3.1`.
- [11:41] Исправление: создан `.npmrc` с `legacy-peer-deps=true` — флаг игнорирует конфликтующие peer-зависимости и применяется как при локальной установке, так и в Docker.
- [11:42] Изменение `Dockerfile`: в `COPY` добавлен `.npmrc` (`COPY package.json package-lock.json .npmrc ./`), чтобы флаг `legacy-peer-deps` применялся на этапе `npm ci` в контейнере.
- [11:42] Конец работы: задача выполнена.
- [12:03] Ошибка: после добавления `.npmrc` с `legacy-peer-deps=true` сборка снова упала на `npm ci` с той же ошибкой "Exit handler never called!". Это известный баг npm 10.x: `npm ci` аварийно завершается при конфликтующих peer-зависимостях, даже с флагом `legacy-peer-deps`.
- [12:03] Исправление `Dockerfile`: команда `npm ci` заменена на `npm install --legacy-peer-deps` — рабочий обходной путь для бага npm 10.x.
- [12:03] Конец работы: задача выполнена.
- [12:22] Ошибка: `npm install --legacy-peer-deps` также упал с ошибкой "Exit handler never called!" — баг npm 10.x не решается флагом `--legacy-peer-deps`.
- [12:22] Исправление `Dockerfile`: установка зависимостей переключена с npm на yarn (`yarn install --frozen-lockfile`), т.к. в проекте уже есть `yarn.lock`. Yarn корректно обрабатывает конфликтующие peer-зависимости пакетов `@alfalab/core-components`.
- [12:22] Конец работы: задача выполнена.
- [12:46] Решение: смена подхода к Docker-развёртыванию — билд собирается локально, а контейнер только раздаёт готовый `dist` через nginx. Это полностью обходит баг npm 10.x «Exit handler never called!» при установке зависимостей внутри контейнера.
- [12:48] Сборка: выполнена команда `npm run build` локально — `dist` создан успешно (index.html, assets/index-*.css, assets/index-*.js).
- [12:49] Изменение `Dockerfile`: убран этап сборки node, остался только `nginx:alpine` с копированием готового `dist` в `/usr/share/nginx/html`.
- [12:49] Изменение `.dockerignore`: `dist` больше не исключается из контекста сборки (копируется в образ как готовый билд).
- [12:50] Изменение `README.md`: раздел «Запуск через Docker» переписан под новый подход — сначала `npm run build`, затем `docker compose up -d --build`.
- [12:50] Конец работы: задача выполнена.
- [12:54] Изменение `README.md`: раздел «Запуск» расширен подробной инструкцией по локальному запуску — требования (Node.js 22.22.0, nvm), установка зависимостей, запуск в dev-режиме, сборка для продакшена и предпросмотр (`npm run preview`). Добавлено пояснение про `.npmrc` с `legacy-peer-deps`.
- [12:54] Конец работы: задача выполнена.
- [13:21] Решение: готовый билд `dist` будет храниться в git, чтобы пользователи могли развернуть приложение через Docker без локальной сборки и без Node.js.
- [13:22] Изменение `.gitignore`: убрано исключение `dist/` — готовый билд теперь попадает в git.
- [13:23] Изменение `README.md`: раздел «Запуск через Docker» упрощён — достаточно `docker compose up -d --build`, без локальной сборки. Добавлен подраздел «Обновление билда» для разработчиков (`npm run build && git add dist`).
- [13:23] Конец работы: задача выполнена.